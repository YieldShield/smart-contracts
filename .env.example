# Template for foundry environment variables.

# For local development, copy this file, rename it to .env, and fill in the values.

# We provide default values so developers can start prototyping out of the box,
# but we recommend getting your own API Keys for Production Apps.

# DEPLOYER_PRIVATE_KEY is used while deploying contract.
# On anvil chain the value of it can be empty since we use the prefunded account
# which comes with anvil chain to deploy contract.
# NOTE: You don't need to manually change the value of DEPLOYER_PRIVATE_KEY, it should
# be auto filled when run `yarn generate`.

# Alchemy rpc URL is used while deploying the contracts to some testnets/mainnets, checkout `foundry.toml` for it's use.
ALCHEMY_API_KEY=your-alchemy-api-key
# Etherscan API key is used to verify the contract on etherscan.
ETHERSCAN_API_KEY=your-etherscan-api-key
# Pyth Hermes authentication is required for every updater by July 31, 2026.
# The updater selects the legacy or upgraded official endpoint from the Pyth
# contract configured in the deployed PythOracle. Set PYTH_HERMES_URL only for
# an explicitly compatible provider or a deliberate cutover override.
PYTH_API_KEY=your-pyth-api-key
# PYTH_HERMES_URL=https://pyth.dourolabs.app/hermes
# Foundry's `robinhood` and `robinhoodTestnet` aliases use these operator-provided
# endpoints for deployments and indexing. The rate-limited public endpoints remain
# available only through the explicit `robinhoodPublic` and `robinhoodTestnetPublic`
# development aliases.
ROBINHOOD_RPC_URL=https://robinhood-mainnet.g.alchemy.com/v2/your-alchemy-api-key
ROBINHOOD_TESTNET_RPC_URL=https://robinhood-testnet.g.alchemy.com/v2/your-alchemy-api-key
# Public deployment manifests are promoted only after this validation endpoint
# agrees with the deployment RPC on the exact finalized block, receipts, runtime
# code, and protocol wiring. It must resolve to a distinct provider host operated
# by a different organization.
# YS_DEPLOYMENT_VALIDATION_RPC_URL=https://independent-rpc.example/v1/your-api-key
# Explicit non-secret operator slugs are required and persisted in finality
# evidence. Identify the organization operating each endpoint, not its product,
# account, region, or hostname. Values are normalized to lowercase and must differ.
# This is operator-attested evidence; prefer a self-hosted full node for one side.
# YS_DEPLOYMENT_RPC_OPERATOR=alchemy
# YS_DEPLOYMENT_VALIDATION_RPC_OPERATOR=self-hosted-berlin
# Optional deploy helper defaults. Set this to make
# `yarn deploy --network robinhoodTestnet` use the funded test keystore without a prompt.
# ROBINHOOD_TESTNET_KEYSTORE_ACCOUNT=test
# ETH_KEYSTORE_ACCOUNT=test

# Robinhood testnet defaults to relaxed governance guardrails so a funded test account
# can deploy without precomputed production Safe confirmations. Reviewed runtime
# codehash pins remain mandatory because no public manifest can be promoted without them.
# Set true when rehearsing all stricter production checks on chain 46630.
# YS_ROBINHOOD_TESTNET_STRICT_PRODUCTION_GUARDS=false

# Production deploy script guardrails. Compute and review these from a deterministic
# rehearsal before deploying: cast codehash <address> --rpc-url <rpc-url>.
# The governor pin must be the exact deployed runtime from that rehearsal because its
# token and timelock constructor immutables are patched into the runtime bytecode.
# YS_PRODUCTION_FACTORY_PROXY_CODEHASH=
# YS_PRODUCTION_FACTORY_IMPLEMENTATION_CODEHASH=
# YS_PRODUCTION_POOL_IMPLEMENTATION_CODEHASH=
# YS_PRODUCTION_YS_TOKEN_CODEHASH=
# YS_PRODUCTION_TIMELOCK_CODEHASH=
# YS_PRODUCTION_GOVERNOR_CODEHASH=
# YS_PRODUCTION_COMPOSITE_ORACLE_CODEHASH=
# YS_PRODUCTION_ERC4626_ORACLE_CODEHASH=
# YS_PRODUCTION_PYTH_ORACLE_CODEHASH=
# Arbitrum One production broadcasts require an explicit acknowledgement that
# the configured Pyth updater remains trusted to publish accepted price updates.
# YS_PRODUCTION_PYTH_UPDATER_CONFIRMED=false
# YS_PRODUCTION_CHAINLINK_ORACLE_CODEHASH=
# YS_PRODUCTION_US_MARKET_SESSION_GATE_CODEHASH=
# Pin the RobinhoodStockOracleFeed runtime from the same deterministic rehearsal.
# Its immutable inner Chainlink feed and market-session gate addresses make this
# codehash deployment-specific; manifest promotion verifies both immutables too.
# YS_PRODUCTION_ROBINHOOD_STOCK_ORACLE_CODEHASH=
# Required for every Robinhood production-script deployment, including relaxed
# testnet runs. Use an operational signer or multisig that is not the governance
# timelock. It can pause the US-equity session gate immediately, but cannot
# unpause it or change the calendar.
# YS_PRODUCTION_MARKET_SESSION_GUARDIAN=

# Robinhood mainnet fails closed unless a verified, code-bearing sequencer
# uptime feed, its public provenance, and its reviewed runtime pin are supplied.
# The source value is written
# to deployment metadata; use a documentation URL or release identifier, never a secret.
# YS_ROBINHOOD_SEQUENCER_FEED=
# YS_ROBINHOOD_SEQUENCER_FEED_SOURCE=
# Pin the exact deployed runtime of that operator-reviewed mainnet feed. No
# canonical Robinhood feed is currently documented by Chainlink; mainnet stays
# blocked until an operator supplies and reviews all three values.
# YS_ROBINHOOD_SEQUENCER_FEED_CODEHASH=
# Robinhood testnet may use a published feed and optional provenance.
# YS_ROBINHOOD_TESTNET_SEQUENCER_FEED=
# YS_ROBINHOOD_TESTNET_SEQUENCER_FEED_SOURCE=
# This exception is honored only on Robinhood testnet (chain ID 46630), including
# strict rehearsals. It can never disable the Robinhood mainnet sequencer guard.
YS_ROBINHOOD_ALLOW_MISSING_SEQUENCER_FEED=false
# Demo seeding is disabled by default in both relaxed and strict testnet modes.
# Set true explicitly for a product-loop rehearsal with mock assets, feeds, pools, and seed liquidity.
# YS_ROBINHOOD_TESTNET_SEED_DEMO_ASSETS=true
# Default account for localhost / use "scaffold-eth-custom" if you wish to use a generated account or imported account
LOCALHOST_KEYSTORE_ACCOUNT=scaffold-eth-default
